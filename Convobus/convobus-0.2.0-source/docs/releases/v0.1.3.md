# Convobus 0.1.3

Convobus 0.1.3 is the initial self-contained open-source release of the native
macOS provider → project → route experience.

## Highlights

- Install from a standard DMG by dragging Convobus into Applications.
- Run without installing Node.js, a programming language, a package manager, or
  any other supporting runtime.
- Use the same app on Apple-silicon and Intel Macs running macOS 13 or newer.
- Organize conversations by provider and project, then select App or CLI and an
  exact provider-specific route.
- Switch providers and projects with stable single-click selection and restore
  each provider's most recently used project and route.
- Keep Convobus available from the macOS menu bar after closing its window.
- Retain the existing local APIs, logs, CLI commands, card lifecycle, and folder
  binding behavior.

## Install

1. Download `Convobus-0.1.3-macOS-universal.dmg`.
2. Verify it against `SHA256SUMS.txt` if desired.
3. Open the DMG and drag `Convobus.app` onto **Applications**.
4. Eject the disk image and open Convobus from Applications.

This public build is ad-hoc signed and is not Developer ID signed or notarized.
macOS may block its first launch. After trying to open the app, follow
[Apple's Open Anyway instructions](https://support.apple.com/guide/mac-help/open-a-mac-app-from-an-unknown-developer-mh40616/mac)
only if you trust the download.

## Requirements

- macOS 13 or newer
- Apple-silicon or Intel Mac
- The provider apps or CLI tools for the routes you intend to use
- Accessibility permission for routes that interact with an app UI

Node.js 22.23.2 LTS is embedded inside the app and requires no separate install.

## Known release constraints

- The release is not notarized, so first-launch approval is expected.
- There is no automatic updater in this release.
- Grok currently provides a CLI route; an App route is intentionally unavailable.
